### 聊天室

**前端处理**

```js
window.onload=function(){
    //连接服务器
    var ws=new WebSocket("ws://localhost:8090")
    ws.onopen=function(){
		alert('ws 已打开')
    }
    ws.onerror=function(){
        alert('ws 连接发生错误')
    }
    ws.onmessage=function(data){
		alert(data)
    }
}
```

**后端处理**

