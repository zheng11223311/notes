**react-router-dom 相关API**

react-router 有web（要讲的），native（操作原生），any（使用在任何地方）

route 路由

router 路由器

**1.内置组件**

```
1.<BrowserRouter>
2.<HashRouter>
3.<Route>
4.<Redirect>
5.<Link>
6.<NavLink>
7.<Switch>
```

**2.其他**

1.history 对象

2.match 对象

3.withRouter 函数

**路由的基本使用**

**下载:**

```
npm install --save react-router-dom
```

**引在.jsx 文件使用注释,快捷键ctrl+shift+/**

**示例**

```js
/*App,js 文件*/
import './App.css';
import { Link, BrowserRouter,Route} from 'react-router-dom'
import Home from './components/Home'
import About from './components/About'

function App() {
  return (
    <div className="App">
         <div className='row'>
                    <div className="col-xs-offset-2 col-xs-8">
                        <div className="page-header">
                            <h2>React Router Demo</h2>
                        </div>
                    </div>
                </div>
                <div className='row'>
                    <div className="col-xs-offset-2 col-xs-2">
                        <div className="list-group">
                            {/* 原生html 中，靠<a> 标签跳转不同的页面 */}
                            {/* 在React 中靠路由链接实现切换组件--编写路由链接 */}
                            {/* <BrowserRouter>   Link 和Router 都要在一个
                            BrowserRouter 路由器下管理，所以将路由器写在App 外面
                            */}
                                <Link className="list-group-item" to="/about">About</Link>
                                <Link className="list-group-item" to="/home">Home</Link>
                            {/* </BrowserRouter> */}

                        </div>
                    </div>
                </div>
                <div className=" col-xs-6">
                    <div className="panel">
                        <div className="panel-body">
                            {/* 注册路由 */}
                            {/* <BrowserRouter> */}
                            <Route path="/about" component={About}/>
                            <Route path="/home" component={Home}/>
                            {/* </BrowserRouter> */}
                        </div>
                    </div>
                </div>
    </div>
  );
}

export default App;

```

```js
//index.js 文件
import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';
import {  BrowserRouter} from 'react-router-dom'

ReactDOM.render(
  <React.StrictMode>
    <BrowserRouter>
    <App />
    </BrowserRouter>
  </React.StrictMode>,
  document.getElementById('root')
);
reportWebVitals();
```

```jsx
{/*Home.jsx 文件*/}
import React, { Component } from 'react'


export default class Home extends Component {
    render() {
        return (
            <div>

                <h1>我是路由Home</h1>
            </div>
        )
    }
}
```

```jsx
{/*About 文件*/}
import React, { Component } from 'react'

export default class About extends Component {
    render() {
        return (
            <div>
                <h1>我是路由About</h1>
            </div>
        )
    }
}

```

