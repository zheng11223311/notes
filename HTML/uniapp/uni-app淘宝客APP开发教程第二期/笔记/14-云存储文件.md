# 云存储文件

```vue
<template>
	<view class="content">
		<button type="default" @click="open">查询</button>
		<!-- 调用云存储的图片 -->
		<!-- <image src="https://vkceyugu.cdn.bspapp.com/VKCEYUGU-1822eabc-7e4a-485f-8097-d3e80c117aaf/5ef45993-9d58-4f36-92b5-21bc207366ff.png" mode=""></image> -->
		<image :src="src" mode=""></image>

	</view>
</template>

<script>
	export default {
		data() {
			return {
				title: 'Hello',
				src:""
			}
		},
		onLoad() {

		},
		methods: {
			open() {
				// 演示云存储 上传云的过程
				// 选择图片弹框
				// uni.chooseImage({
				// 	count:1, //限制一张图片
				// 	success:(res)=>{
				// 		console.log(res);
				// 		// 获取文件临时路径
				// 		let filePath=res.tempFilePaths[0]
				// 		console.log('图片地址' ,filePath);
						
				// 		// 上传到云存储
				// 		uniCloud.uploadFile({
				// 			cloudPath:"1.jpg",		//上传后的云端文件路径
				// 			filePath:filePath, //要上传的文件的文件路径
				// 			success:(res)=> {
				// 				console.log('成功的返回 ',res);
				// 				console.log(this);
				// 				// this.src=res.fileID	//云端文件位置
				// 				this.src=res.filePath	 //本地blob流 位置,刷新后消失
				// 			},
				// 			fail(e) {
				// 				console.log(e);
				// 			}
				// 		})
				// 	},
				// 	fail(e) {
				// 		console.log(e);
				// 	}
				// })
				
				
				// 删除云存储
				// 前端删除云存储是一个危险的操作,
				// 阿里云不支持此操作,执行就会报错,
				// 因该由云函数进行校验,由云函数删除
				// 鼠标悬浮代码上方,进入官网对应的文档
				uniCloud.deleteFile({
				})
				
			}
		}
	}
</script>

<style>

</style>

```

